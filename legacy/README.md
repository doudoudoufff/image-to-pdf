# 旧版 Python / tkinter 实现

本目录为改造前基于 **Python 3**、**tkinter**、**Pillow**、**reportlab**、**PyPDF2** 的桌面工具源码，已不再维护。

当前项目的主程序为仓库根目录下 **`app/`** 中的 **Flutter 桌面应用**（Windows / macOS）。

若仍需运行旧版：

```bash
cd legacy
pip install -r requirements.txt
python main.py
```
