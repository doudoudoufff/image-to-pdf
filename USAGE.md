# 使用说明（Flutter 桌面版）

## 快速开始

### Windows

1. 从 GitHub Releases 下载 Windows 压缩包并解压。
2. 在解压目录中运行 `image_to_pdf.exe`（若被杀毒软件拦截，请按需加入白名单）。

### macOS

1. 从 Releases 下载 macOS 压缩包并解压。
2. 打开 `image_to_pdf.app`。
3. 若系统提示「无法打开」，请在 Finder 中对该应用 **按住 Control 点按 → 打开**，在对话框中确认打开。

## 操作步骤

1. 启动应用。
2. 点击 **「添加图片」**，可多选；可**反复添加**，从不同文件夹加入图片（同一文件路径不会重复添加）。
3. 在列表中拖拽左侧 **手柄** 调整输出顺序。
4. 点击某条右侧 **删除** 可移除该图。
5. 点击 **「导出 PDF」**，选择保存位置与文件名，等待进度完成。

## 版式说明

- 每张图片单独一页。
- 宽大于等于高时使用 **A4 横版**，否则 **A4 竖版**。
- 图片在页边距内等比例缩放以完整显示；**不会放大**超过原图分辨率对应尺寸。
- 页面底部为文件名区域，中英文均可显示（依赖内置 Noto 字体）。

## 常见问题

**无法添加某些文件**  
仅支持 PNG、JPEG、BMP、GIF；文件损坏或格式异常时会被跳过，若全部失败会提示错误。

**PDF 体积较大**  
默认使用 PNG 嵌入以保证兼容；极大图片会在内存中先限制长边像素以控制体积（见源码 `ImagePipeline`）。

**旧版 Python 程序**  
若需运行历史实现，见仓库 [`legacy/README.md`](legacy/README.md)。
